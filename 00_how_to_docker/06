#search for and pull the NGINX official image from docker hub
# https://www.docker.com/blog/tips-for-deploying-nginx-official-image-with-docker/
# -d option run container in the background and print the container id
# -p option publish a container's port/s to the host

docker run -d --name overlord --restart always -p 5000:80 nginx
